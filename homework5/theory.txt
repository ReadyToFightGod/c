1. Что такое идиома RAII? Как расшифровывается эта аббревиатура?
RAII - Resource Acquisition is Initialization – захват ресурса есть инициализация. Смысл в том, что что с помощью каких-нибудь механизмов получение 
некоторого ресурса неразрывно совмещается с инициализацией, а освобождение — с уничтожением объекта. Если кратко, то в конструкторе объект получает 
доступ к чему-либо (например, открывается файл) и сохраняет описатель этого ресурса в закрытый члена класса (инициализация), а при вызове деструктура 
(удаление объекта) этот ресурс освобождается (закрывается файл).

2. Что такое конструктор, копирующий конструктор, деструктор, геттер, сеттер? Как определить конструктор и деструктор по умолчанию?
Конструктор — это особый тип метода класса, который автоматически вызывается при создании объекта этого же класса. Конструкторы обычно 
используются для инициализации переменных-членов класса значениями, которые предоставлены по умолчанию/пользователем. Конструктор, который не имеет параметров 
или содержит параметры со значениями по умолчанию, называется конструктором по умолчанию. Задать его можно в части класса public: Class_name(type_arg1 arg1,..){
this->arg1 = new type_arg(arg);...; (или new[] для массива)} или просто Class_name() = default (пустой конструктор - чтобы просто создвть тип переменной, не задавая значения полей)
Конструктор копирования (или копи-конструктор) - специальный конструктор, применяемый для создания нового объекта как копии уже существующего (на вход 
принимает константную ссылку на объект\класс).
Деструктор - метод  класса, который выполняет освобождение использованных объектом ресурсов и удаление нестатических переменных объекта. Деструктор не имеет 
возвращаемого значения и не принимает параметров. Каждый класс может иметь только один деструктор. Задается деструктор в области public: ~Class_name(){delete arg1,..;
delete[] arg,..; (для массива)} Если деструктор не определен, то компилятор предоставит его по умилчанию.
Геттер - метод  класса, который позволяет вернуть поле из класса (можно автоматически сгенерить, нажав alt+insert)
Сеттер - метод  класса, который позволяет менять значения поля класса

3. Почему поля класса принято делать приватными? Какой метод называется константным?
Поля класса принято делать приватными, чтобы простые пользователи не могли самостоятельно менять значение полей (защита данных). Также инкапсулированные
классы (с сокрытой информацией о деталях реализации) упрощают программу (не нужно знать как этот класс работает "под капотом").
Константный метод — это метод, который гарантирует, что не будет изменять объект или вызывать неконстантные методы класса (поскольку они могут изменить объект).
Чтобы сделать метод константным, надо добавить ключевое слово const в его объявление.

4. С какой целью проекты делают многофайловыми? Почему происходит разделение кода на .h файлы и .cpp файлы?

5. Какие три этапа проходит код при превращении в исполняемый файл? Расскажите, что в них происходит.
